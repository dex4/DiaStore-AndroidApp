<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data class=".EntryFieldBinding" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/entry_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/Body2.Bold"
            android:textColor="@color/colorPrimary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Carbs" />

        <TextView
            android:id="@+id/entry_value"
            android:layout_width="@dimen/entry_field_value_size"
            android:layout_height="@dimen/entry_field_value_size"
            android:background="@drawable/bg_entry_field"
            android:gravity="center"
            android:maxLines="1"
            android:textAppearance="@style/Body2"
            android:textColor="@color/white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@id/entry_name"
            app:layout_constraintStart_toStartOf="@id/entry_name"
            app:layout_constraintTop_toBottomOf="@id/entry_name"
            tools:text="45" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>